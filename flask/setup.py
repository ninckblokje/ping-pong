from setuptools import setup

setup(
   name='ping_pong',
   version='1.0',
   description='A useful module',
   author='ninckblokje',
   author_email='jeroen dot ninckblok at gmail dot com',
   install_requires=['Flask'],
)